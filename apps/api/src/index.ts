// @trachex/api
//
// Phase 6 implements the Hono local server here, started by `trachex
// dashboard`, serving the built dashboard assets and the /api routes from
// docs/architecture.md. Handlers validate input, call domain services, and
// serialize domain results; the browser never opens SQLite directly.
