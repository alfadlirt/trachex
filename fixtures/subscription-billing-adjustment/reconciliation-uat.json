{
  "kind": "reconciliation",
  "create": [
    {
      "title": "Make conversion retries idempotent",
      "description": "A retry or duplicated webhook must never create a second charge.",
      "sourceLocation": "findings",
      "parentLabel": "Payment recovery",
      "impacts": [{ "kind": "service", "value": "payment-retry-worker" }],
      "scenarios": ["A duplicated provider event creates no second charge."]
    },
    {
      "title": "Show the conversion date in the customer timezone",
      "description": "Invoice and account history use the timezone captured at checkout.",
      "sourceLocation": "findings",
      "parentLabel": "Payment recovery",
      "impacts": [{ "kind": "page", "value": "/account/billing" }]
    }
  ]
}
