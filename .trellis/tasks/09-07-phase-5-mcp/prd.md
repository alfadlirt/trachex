# Phase 5: MCP

## Goal

Stdio server via trachex mcp --project; read + mutation tools from architecture contract; Zod schemas; explicit confirmation for human-completion mutation; structured errors and stable tool descriptions. Acceptance: MCP client retrieves full baseline; agent creates adjustment proposal and inspects status; agent cannot mark complete without explicit confirmation; MCP cannot cross project boundary.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
