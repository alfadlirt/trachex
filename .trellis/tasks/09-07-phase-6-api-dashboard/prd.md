# Phase 6: Local API and Bundled Dashboard

## Goal

Hono server via trachex dashboard; static serving of built React app; project list + ticket navigation; ticket canvas with checklist as main artifact; proposal review/approval; inline adjustment flow; timeline/history + impact panels; export controls; embedded chatbot on same services; responsive mobile layout. Acceptance: works with SQLite no Docker; browser never touches SQLite directly; dashboard+CLI concurrent; completion always human; chat changes appear as pending proposals.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
