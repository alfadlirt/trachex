// @trachex/domain
//
// Phase 1 defines the domain here: entities (Project, Repository, Ticket, Source,
// Requirement, Proposal, ...), repository interfaces, domain commands, and
// invariants (append-only requirements, proposal approval as the only apply
// path, human completion audit, project scoping).
//
// This package depends on nothing platform-specific and must not expose
// SQLite types (ADR 001).
